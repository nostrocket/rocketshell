interface:
  display_name: "Commit Token Cost"
  short_description: "Estimate USD and sat cost per Git commit"
  default_prompt: "Use $commit-token-cost to estimate token usage and API-equivalent USD and sat cost for recent commits."
